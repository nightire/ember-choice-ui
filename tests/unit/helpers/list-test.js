import { list } from 'dummy/helpers/list';
import { module, test } from 'qunit';

module('Unit | Helper | list');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = list([42]);
  assert.ok(result);
});
